/**
 * Shared components contributed by feature modules.
 *
 * Each feature module that needs to share a component across the app adds one
 * file here plus a single `export * from "./<module-id>";` line, keeping
 * cross-branch merge conflicts to a single line.
 */

export {};
