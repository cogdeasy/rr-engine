import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "Cost analytics" };

export default function Page() {
  return <ModulePlaceholder moduleId="costs" />;
}
