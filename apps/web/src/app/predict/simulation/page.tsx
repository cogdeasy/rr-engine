import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "What-if simulation" };

export default function Page() {
  return <ModulePlaceholder moduleId="simulation" />;
}
